/** Value-added tax rate for quotation totals (Vietnam default). */
export const QUOTATION_VAT_RATE = 0.1;

/** Discount ratio above which submitting requires manager approval (0–1). */
export const QUOTATION_DISCOUNT_APPROVAL_THRESHOLD = 0;
